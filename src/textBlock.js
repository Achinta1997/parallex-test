import "./App.css";

function TextBlock(){
    return (
        <div id="textblock">
        <div id="textblock-container">
            <h1 id="textblock-title">Parallax</h1>
            <p id="textblock-content">
            Parallax, in astronomy, the difference in direction of a celestial object as seen by an observer from two widely separated points.</p>
        <br></br>
        <p id="textblock-content"> Whereas, Parallax scrolling is a web site trend where the background content (i.e. an image) is moved at a different speed than the foreground content while scrolling.
        </p> 
        </div>
        </div>
    )
}

export default TextBlock;

